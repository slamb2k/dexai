# =============================================================================
# DexAI Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit the .env file to version control!
#
# Variables are organized by category with required/optional status.
# =============================================================================

# =============================================================================
# SECURITY - Master Encryption Key (REQUIRED for vault operations)
# =============================================================================
# This key encrypts all secrets in the vault (data/vault.db).
# Generate a strong, unique password and keep it safe.
# If lost, you will need to re-enter all stored secrets.
DEXAI_MASTER_KEY=your-secure-master-password-here

# =============================================================================
# AI PROVIDERS - LLM API Keys
# =============================================================================

# Anthropic Claude API (REQUIRED for task decomposition, friction solving)
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# OpenAI API (REQUIRED for memory embeddings and semantic search)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenRouter API (OPTIONAL - for multi-model routing)
# Enables access to 100+ models from various providers (GPT-4, Gemini, Claude, etc.)
# Get your key at: https://openrouter.ai/keys
# OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key-here

# Google Gemini API (OPTIONAL - for direct Gemini access)
# Get your key at: https://aistudio.google.com/app/apikey
# GOOGLE_API_KEY=AIza-your-google-api-key-here

# Helicone API (OPTIONAL - for LLM observability and cost tracking)
# Get your key at: https://helicone.ai/
# HELICONE_API_KEY=your-helicone-api-key-here

# =============================================================================
# MESSAGING CHANNELS - Bot Tokens
# =============================================================================
# These can also be stored in the encrypted vault (recommended for production).
# Use: python tools/channels/{platform}.py --set-token YOUR_TOKEN
#
# Environment variables are used as fallback when vault access fails.

# Telegram Bot (OPTIONAL - required only if using Telegram)
# Create a bot via @BotFather on Telegram
# TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrSTUvwxYZ

# Discord Bot (OPTIONAL - required only if using Discord)
# Create a bot at: https://discord.com/developers/applications
# DISCORD_BOT_TOKEN=your-discord-bot-token-here

# Slack App (OPTIONAL - required only if using Slack)
# Create an app at: https://api.slack.com/apps
# Bot token starts with xoxb-
# SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
# App-level token starts with xapp- (required for Socket Mode)
# SLACK_APP_TOKEN=xapp-your-slack-app-token

# =============================================================================
# AUTOMATION - Webhook Security
# =============================================================================

# Webhook Secret (OPTIONAL - for securing incoming webhooks)
# Used to validate webhook payloads from external services
# WEBHOOK_SECRET=your-webhook-secret-here

# =============================================================================
# DEPLOYMENT - Docker & Reverse Proxy
# =============================================================================

# Domain for HTTPS (used by Caddy)
# For local development, leave as localhost
# For production, set your domain: dexai.example.com
DEXAI_DOMAIN=localhost

# Admin email for Let's Encrypt certificates
DEXAI_ADMIN_EMAIL=admin@example.com

# Caddy TLS mode (default: local_certs for self-signed; remove for Let's Encrypt auto)
# CADDY_TLS_MODE=local_certs

# Port overrides (optional, defaults shown)
# DEXAI_BACKEND_PORT=8080
# DEXAI_FRONTEND_PORT=3000

# Tailscale auth key (required for --profile tailscale)
# Generate at: https://login.tailscale.com/admin/settings/keys
# TAILSCALE_AUTHKEY=tskey-auth-xxx

# Tailscale machine name (default: dexai)
# This becomes your URL: https://<hostname>.<tailnet>.ts.net
# TAILSCALE_HOSTNAME=dexai

# =============================================================================
# LIMITS - Cost & Rate Limiting
# =============================================================================

# Maximum daily API spend in USD (default: 10)
# DAILY_COST_LIMIT_USD=10

# Maximum tokens per request (default: 4096)
# MAX_TOKENS_PER_REQUEST=4096

# =============================================================================
# DEVELOPMENT & DEBUGGING
# =============================================================================

# Python Environment (OPTIONAL)
# Uncomment to set Python unbuffered mode for logging
# PYTHONUNBUFFERED=1

# Debug Mode (OPTIONAL - enables verbose logging)
# DEXAI_DEBUG=true

# Log Level (OPTIONAL - DEBUG, INFO, WARNING, ERROR, CRITICAL)
# DEXAI_LOG_LEVEL=INFO

# Environment identifier (development, staging, production)
# DEXAI_ENV=development

# =============================================================================
# NOTES
# =============================================================================
#
# 1. SECURITY BEST PRACTICES:
#    - Never commit .env to git (it's in .gitignore)
#    - Use the vault for production secrets: python tools/security/vault.py
#    - Rotate keys periodically
#    - Use different keys for dev/staging/production
#
# 2. REQUIRED vs OPTIONAL:
#    - DEXAI_MASTER_KEY: Required for vault encryption
#    - ANTHROPIC_API_KEY: Required for AI task features
#    - OPENAI_API_KEY: Required for memory/semantic search
#    - Channel tokens: Only required if using that channel
#
# 3. VAULT INTEGRATION:
#    The system prefers secrets from the encrypted vault (data/vault.db).
#    Environment variables are used as fallback.
#
#    To store a secret in the vault:
#    python tools/security/vault.py --action set --key API_KEY --value "secret"
#
#    To list stored secrets:
#    python tools/security/vault.py --action list
#
# 4. CHANNEL SETUP:
#    Each channel has its own setup process:
#    - Telegram: Talk to @BotFather, get token, run --set-token
#    - Discord: Create app in developer portal, enable intents
#    - Slack: Create app, enable Socket Mode, get both tokens
#
# =============================================================================
